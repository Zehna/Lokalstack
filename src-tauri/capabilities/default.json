{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. Phase 0 grants only core defaults; powerful plugins are added per-phase later.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
