import { PlaceholderPage } from '@/features/services/ServicesPage'

/** Workspaces view — Phase 0 placeholder. Workspaces land in Phase 6. */
export function WorkspacesPage() {
  return (
    <PlaceholderPage
      title="Workspaces"
      description="Groups of related services that typically belong to the same project."
    />
  )
}
