import { PlaceholderPage } from '@/features/services/ServicesPage'

/** History view — Phase 0 placeholder. Event history arrives with Phase 10. */
export function HistoryPage() {
  return (
    <PlaceholderPage
      title="History"
      description="Past observations: services that came and went, conflicts, health changes."
    />
  )
}
