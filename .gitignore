# Dependencies
node_modules/

# Frontend build output
dist/

# Rust / Tauri build output
src-tauri/target/
src-tauri/gen/

# Editor / OS noise
.DS_Store
Thumbs.db
*.local
*.log

# Coverage output
coverage/

# Release artifacts (binaries/installers stay out of git; the checksum
# manifest is regenerated per build and the hashes live in RELEASE_CHECKLIST
# / the release report)
release-artifacts/
*.exe
*.msi
msi_install.log

# Locally downloaded CI release-candidate artifact sets (Phase 11A) — these
# are read-only QA inputs and must never be committed.
github-release-artifacts/
